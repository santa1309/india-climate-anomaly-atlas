# Runtime / build logs
generation_run.log
*.log

# Python caches
__pycache__/
*.pyc

# Editor / tooling
.claude/
.vscode/
.DS_Store

# Intermediate artifacts written during geometry export
data/temp_districts.geojson

# Offline standalone build (~64 MB, regenerable, NOT served by Pages).
# Kept out of git so the weekly data push never hits GitHub's 100 MB limit.
# Build it locally with: python update_atlas.py --build
dashboard_standalone.html

# Source rainfall archive for the 1971-2020 LPA normal (~6.4 GB daily NetCDF).
# Local input only; must never be committed (exceeds GitHub's 100 MB limit).
rainfall_historic/
*.nc

# Promo media in the SatSure dashboard (100s of MB; not served by Pages).
*.mp4
*.gif
